import 'package:flutter/material.dart';

AppBar defaultAppBar(BuildContext context) {
  return AppBar(
    // TODO: Customize me yo.
    title: Text('TripMeOut'),
    // Don't show app bar back button, it looks gross.
    leading: Container(),
  );
}
